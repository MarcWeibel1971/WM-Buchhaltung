import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
export function BankImportDeleteConfirmDialog({item,pending,onClose,onConfirm}:{item:{batchId:string;filename:string;count:number}|null;pending:boolean;onClose:()=>void;onConfirm:(batchId:string)=>void}){return <Dialog open={!!item} onOpenChange={open=>{if(!open)onClose()}}><DialogContent className="sm:max-w-md"><DialogHeader><DialogTitle className="text-destructive">Import rükgängig machen?</DialogTitle><DialogDescription>Alle <strong>{item?.count??0} Transaktionen</strong> aus dem Import<br/><span className="font-medium text-foreground">{item?.filename}</span><br/>werden unwiderruflich gelöscht. Bereits verbuchte Transaktionen können nicht gelöscht werden.</DialogDescription></DialogHeader><DialogFooter><Button variant="outline" onClick={onClose}>Abbrechen</Button><Button variant="destructive" disabled={pending} onClick={()=>{if(item)onConfirm(item.batchId)}}>{pending?"Lösche...":"Import löschen"}</Button></DialogFooter></DialogContent></Dialog>}
