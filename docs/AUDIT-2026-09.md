# Audit WM-Buchhaltung – September 2026

Stand: Branch `claude/wm-buchhaltung-audit-oj5u1j`, Basis `main` @ 6be8b90 (nach AP1–AP4).

Ausgangslage: Typecheck grün, 306 Unit-Tests grün (21 übersprungen, benötigen DB/Resend),
Build grün. Der Audit umfasste Backend (Sicherheit, Mandantentrennung, Buchhaltungslogik),
Frontend, Build/Deployment und Projekt-Konfiguration. Befunde sind nach Schweregrad
sortiert; die Spalte «Status» zeigt, was in diesem Branch behoben wurde.

Legende Status: ✅ behoben · 🟡 teilweise / abgemildert · ⏳ offen (Vorschlag unten)

## 1. Sicherheit & Mandantentrennung

| # | Schwere | Befund | Status |
|---|---|---|---|
| S1 | Kritisch | `auth.register` / `forgotPassword` / `resendVerification` / `invitations.create` / Stripe-Checkout bauten Links aus dem **Client-Origin** (`input.origin`). Angreifer konnte für fremde Konten einen Passwort-Reset auslösen, dessen Link auf seine Domain zeigt → Token-Diebstahl, Kontoübernahme. | ✅ `server/_core/publicUrl.ts` (`APP_URL`, sonst nur Request-Origin), Tests `server/publicUrl.test.ts` |
| S2 | Kritisch | `routers.ts`: Journal (approve/reject/update/delete/revert/bulk/export), Bank-Import, Kreditkarten-Abrechnungen, Belege (14 Prozeduren), MWST-Perioden, Lohn: Abfragen nur nach `id` ohne `organizationId` → jeder eingeloggte Nutzer konnte Daten **aller Mandanten** lesen, freigeben, ändern, löschen. | ✅ Alle betroffenen Prozeduren und db.ts-Helfer (`orgId` als Pflichtparameter) org-gefiltert; 22 neue Tests in `server/tenancy.test.ts` |
| S3 | Kritisch | `ebicsRouter.ts`: `protectedProcedure`, `organizationId: 1` hart codiert, keine Org-Filter → EBICS-Zugänge aller Mandanten sichtbar/löschbar, Schlüssel neu generierbar. | ✅ `orgProcedure`, Org-Filter überall, Löschen/Schlüssel nur Org-Admin |
| S4 | Kritisch | `DELETE /api/upload/document/:id` löschte beliebige Belege aller Mandanten. | ✅ org-gescoped, ID-Validierung, Datei wird mitgelöscht |
| S5 | Kritisch | `adminProcedure` lässt jeden Org-Owner durch (jeder Nutzer wird beim Anlegen einer Org Owner) → globale Buchungsregeln aller Mandanten manipulierbar. | ✅ neue `platformAdminProcedure` für globale Regeln und `system.notifyOwner` |
| S6 | Hoch | `/manus-storage/*` lieferte Belege, Rechnungs-PDFs und Vorlagen **ohne Authentifizierung** aus. | ✅ Session-Pflicht in `storageProxy.ts` |
| S7 | Hoch | Kein Brute-Force-Schutz für Login/Registrierung/Reset (300 Versuche/min/IP über den allgemeinen API-Limiter). | ✅ eigener Limiter 20/15 min in `_core/index.ts` |
| S8 | Hoch | Session-Cookie `SameSite=None` ohne CSRF-Schutz; die App wird nie cross-site eingebettet (helmet frame-ancestors). | ✅ `SameSite=Lax` |
| S9 | Hoch | SumUp-Webhook: Signaturprüfung nur wenn Header vorhanden, HMAC über re-serialisierten Body, immer erste Org, `timingSafeEqual`-Exception unbehandelt; Buchung Soll = Haben = Ertragskonto. | ✅ Signatur Pflicht, HMAC über Raw-Body, Org-Zuordnung per Signatur; Buchung Bank an Ertrag über `createJournalEntry`/`approveJournalEntry` (6 neue Tests) |
| S10 | Hoch | `/api/upload/voice` und `/transcribe` ohne Authentifizierung. | ✅ Auth-Pflicht für alle Upload-Endpunkte |
| S11 | Hoch | Kein Rollenmodell innerhalb der Org (viewer kann Buchungen freigeben, DSG-Anonymisierung auslösen); Einladungs-Flow ohne `accept`-Prozedur (Feature funktional tot). | 🟡 `invitations.accept` implementiert, `create` nur für Owner/Admin; Rollen (viewer/bookkeeper) in Fachprozeduren weiterhin nicht durchgesetzt (Vorschlag: `orgRoleProcedure(minRole)`) |
| S12 | Hoch | Belegnummern: `LAST_INSERT_ID()` in zweitem Statement auf einem Pool → falsche/doppelte Nummern unter Last. | ✅ `insertId` aus dem INSERT-Result, kein zweites Statement |
| S13 | Hoch | Keine DB-Transaktionen; Bank-/KK-Freigabe legte Einträge als `approved` **vor** Nummernvergabe/Periodensperre an. | 🟡 Einträge werden als `pending` angelegt und erst durch `approveJournalEntry` (Nummer + Periodensperre) freigegeben; echte DB-Transaktionen (`db.transaction()`) weiterhin offen |
| S14 | Mittel | `lastSignedIn` wurde bei **jedem** API-Request geschrieben (ein UPDATE pro Aufruf). | ✅ höchstens alle 10 Minuten |
| S15 | Mittel | Server startete in Produktion ohne `JWT_SECRET` (leeres Signing-Secret). | ✅ `assertRequiredEnv()` beendet den Start |
| S16 | Mittel | Reset-Token ohne Ablaufdatum galt unbegrenzt. | ✅ |
| S17 | Mittel | Stripe-Webhook ohne Idempotenz / `livemode`-Prüfung. | ✅ `livemode`-Prüfung, idempotentes `checkout.session.completed` |
| S18 | Mittel | Upload: Verknüpfungs-IDs ungeprüft, Dateiendung aus Dateiname, Keys nicht org-gescoped. | ✅ IDs validiert und org-geprüft, Endung aus MIME-Whitelist, Keys `documents/<orgId>/…` |
| S19 | Mittel | `auth.register` überschreibt Name/Passwort-Hash bestehender OAuth-Konten ohne Nachweis. | ⏳ Vorschlag: nur Verifizierungs-Mail senden, Daten erst nach Token-Bestätigung schreiben |
| S20 | Mittel | `organizations.create` ohne Plan-Limit und ohne Transaktion. | ⏳ |
| S21 | Niedrig | PII (E-Mail, Rolle, Org) wurde bei jedem Render in `localStorage` geschrieben und nach Logout nicht gelöscht. | ✅ entfernt |
| S22 | Niedrig | Druckfenster MWST/Kontenplan: Firmenname ungeschützt in `document.write` (Org-XSS). | ✅ HTML-Escaping |

## 2. Buchhaltungslogik

| # | Schwere | Befund | Status |
|---|---|---|---|
| B1 | Kritisch | **Eröffnungsbilanz doppelt gezählt**: Eröffnungssaldi wurden in `opening_balances` UND als freigegebener Journal-Eintrag gespeichert; `getAccountBalance()` addiert beides → Bilanz/Kontoblatt zeigten jeden Eröffnungssaldo doppelt. | ✅ Journal-Eintrag entfällt; Migration `0042` räumt bestehende Duplikate auf |
| B2 | Kritisch | **Jahresgewinn auf Aktienkapital**: Saldovortrag suchte Konten 2990/2290/2800; 2990/2290 existieren nicht → Gewinn landete auf 2800. | ✅ 2950, Fallback 2979, sonst Fehler mit Hinweis |
| B3 | Kritisch | `journal.reject` ohne Statusprüfung → verbuchte Einträge mit Belegnummer verschwanden aus Bilanz/ER/MWST (GeBüV-Lücke). | ✅ `rejectJournalEntry` prüft `assertJournalEntryEditable` |
| B4 | Hoch | MWST-Report: Umsatzsteuer auf Bruttobeträgen (Bankimport bucht ohne MWST-Split), Vorsteuer nur aus Zeilen mit `vatAmount` (Bankimport/KK setzen nie eine) → Zahllast systematisch falsch. | ⏳ Vorschlag: MWST-Split beim Bankimport erzwingen bzw. Umsatzsteuer aus `journalLines.vatAmount` / Konto 1170/2200 ableiten |
| B5 | Hoch | MWST-Methode `pauschal` wurde wie effektiv ohne Vorsteuer behandelt; Satz 0 % (Export) landete im 2.6 %-Topf. | ✅ `pauschal` wie `saldo`; Satz 0 → `turnoverExempt`; Kontenabfragen im MWST-Report org-gefiltert |
| B6 | Hoch | Lohnbuchung: Bank wird mit Brutto + AN- **und** AG-Anteilen belastet, Konten 4010/4040/4025 existieren im KMU-Plan nicht. | ⏳ Vorschlag: Soll 5000 Brutto, Soll 5700 AG-Anteile, Haben 2030 Sozialversicherungen, Haben Bank Netto |
| B7 | Hoch | Storno einer Vorjahres-Rechnung unmöglich (`fiscalYear` ≠ Jahr des Buchungsdatums → Exception); Teilbezahlte Rechnung wird komplett gespiegelt. | ⏳ |
| B8 | Hoch | EUR-Rechnungen werden 1:1 als CHF verbucht (Fremdwährungs-Guard nur im Bankimport). | ⏳ Vorschlag: `isSupportedBookingCurrency()` in `invoices.issue` |
| B9 | Hoch | CAMT.053: Gegenpartei bei Gutschriften = eigene Firma (Cdtr statt Dbtr); Sammelgutschriften (mehrere `TxDtls`) nur erster Beleg mit Gesamtbetrag; `PDNG` nicht gefiltert. | ✅ `shared/bankParser.ts`: Gegenpartei richtungsabhängig, ein Datensatz pro `TxDtls`, `PDNG` übersprungen (37 neue Tests) |
| B10 | Hoch | CSV-Import mit getrennten Spalten Gutschrift/Belastung verliert alle Belastungen; kein Quoting. | ✅ Zwei-Spalten-Layouts, RFC-4180-Quoting, robustes `parseAmount()` |
| B11 | Hoch | Abschreibungen/Abgrenzungen im Jahresabschluss nicht idempotent; Abschlussbuchungen ohne Periodensperre. | ✅ Vorschläge idempotent (`suggestionKey`), Buchungen über `assertFiscalYearOpen` + `createJournalEntry`/`approveJournalEntry` |
| B12 | Hoch | Duplikat-Hash beim Bankimport verwirft echte Zwillings-Transaktionen (gleicher Tag/Betrag/Text). | 🟡 Betrag normalisiert; `AcctSvcrRef`/Referenz im Hash offen (Formeländerung würde bereits importierte Auszüge beim Re-Import verdoppeln → Migration nötig) |
| B13 | Mittel | Doppik-Validierung: 1 Rappen Toleranz, `NaN` besteht die Prüfung, Konten fremder Orgs erlaubt. | ✅ Rappen-genauer Vergleich, `Number.isFinite`/`> 0`, Konten gegen Org validiert |
| B14 | Mittel | Payroll: nur KTG **oder** UVG, kein ALV-Höchstlohn, keine NBU/QST, keine Stichtagslogik. | ⏳ |
| B15 | Mittel | Lohnausweis Ziffer 11 ≠ 8 − 9 − 10; Template von externer CloudFront-URL. | ⏳ |
| B16 | Mittel | MWST-Perioden können sich überlappen (Q1 + S1), keine Sperre nach Einreichung. | ⏳ |
| B17 | Mittel | Rechnungs-MWST zeilenweise gerundet, keine 5-Rappen-Rundung; QR-Betrag ungerundet. | ⏳ |
| B18 | Mittel | QR-Generator: Referenz aus `Date.now() % 100000` (Kollisionen), nicht persistiert → CAMT-Abgleich kann nie treffen. | ⏳ Vorschlag: nur `invoices.issue`-Pfad verwenden |
| B19 | Mittel | MT940: gültige `:61:`-Varianten (RC/RD, Funds-Code, ohne Dezimalen) übersprungen; Währung fix CHF. | ✅ Regex erweitert, Währung aus `:60F:`, mehrzeilige `:86:` |
| B20 | Mittel | Datumsarithmetik via `toISOString()` (UTC) – Server und Client: Fälligkeiten/Buchungsdatum je nach Zeitzone um einen Tag verschoben. | 🟡 Client: `lib/date.ts` (`todayISO`) an 12 Stellen; Server ⏳ |
| B21 | Niedrig | Onboarding bietet Saldosteuersatz 3.7 % (alter Satz); Saldosteuersatz-Default 6.2 hart codiert. | ⏳ |

## 3. Frontend

| # | Schwere | Befund | Status |
|---|---|---|---|
| F1 | Hoch | `OrgGuard`: Mutation im Render, `listMine` nicht invalidiert → Endlos-Spinner bzw. Mutations-Schleife bei Fehler. | ✅ Effekt mit Guard-Ref, Invalidierung, Fehlerzustand mit Retry |
| F2 | Hoch | Betragsfelder: `parseFloat("1'234.50")` = 1, Roh-String ging an den Server. | ✅ `lib/amount.ts` (`parseSwissAmount`), Journal + Zeiterfassung umgestellt |
| F3 | Hoch | Ganzes Frontend in einem 2.4-MB-Bundle; Manus-Runtime (~300 kB) inline in `index.html` auch in Produktion (CSP-Verstoss). | ✅ `React.lazy` für 16 Seiten, Manus-Plugins nur im Dev; `index.html` 368 kB → 1.6 kB |
| F4 | Hoch | `QueryClient` ohne Defaults (Refetch-Sturm bei Fensterfokus); 16 Mutations ohne `onError`. | ✅ Defaults + globaler Fehler-Toast |
| F5 | Hoch | Einladungs-Flow: `returnPath`/`invitationToken` nie ausgewertet. | ✅ `returnPath`/Token werden ausgewertet, eingeloggte Nutzer sehen «Einladung annehmen» |
| F6 | Mittel | Layout lädt auf jeder Seite komplette Listen nur für Badge-Zähler. | ⏳ Vorschlag: Count-Prozeduren |
| F7 | Mittel | `DocumentList`-`refreshKey` ohne Wirkung (leerer Block). | ✅ |
| F8 | Mittel | Destruktive Aktionen ohne Bestätigung (Mitarbeiter deaktivieren, Versicherungssatz/Regel löschen, Jahr abschliessen). | ⏳ |
| F9 | Mittel | `NaN` im Formular-State (Mahngebühr, Stunden/Stundensatz). | ✅ |
| F10 | Mittel | ~2'700 Zeilen toter Code (Avatar/Manus/Map/DashboardLayout/CreditCard). | ✅ entfernt |
| F11 | Mittel | Hardcodierte Jahreslisten und MWST-Sätze in 6 Komponenten, 16 duplizierte `formatCHF`. | ⏳ Vorschlag: `shared/vat.ts`, `lib/format.ts` |
| F12 | Niedrig | 269 `any`-Verwendungen, `lang="en"`, `maximum-scale=1`, Stack-Trace im ErrorBoundary. | ⏳ |

## 4. Build, Deployment, Projekt-Konfiguration

| # | Schwere | Befund | Status |
|---|---|---|---|
| I1 | Hoch | Docker: `/app/data/uploads` gehört root, kein Volume → **jeder Upload scheitert mit EACCES**, Daten gingen beim Neuerstellen verloren. | ✅ Dockerfile + `uploads_data`-Volume + `LOCAL_STORAGE_DIR` |
| I2 | Hoch | `CLAUDE.md`, `.rippletide/selected-rules.md`, `.claude/hooks/check-code.sh` beschrieben ein **anderes Projekt** (FinanzPlan, Next.js/Clerk/oRPC). | ✅ neu geschrieben für diesen Stack |
| I3 | Mittel | `.gitignore`: Eintrag `data/uploads/` durch literale `\n` unwirksam → hochgeladene Finanzbelege konnten ins Repo gelangen. | ✅ |
| I4 | Mittel | Compose-Migrate lief `drizzle-kit generate && migrate` (erzeugt Migrationsdateien im Container). | ✅ nur `migrate` |
| I5 | Mittel | 13 ungenutzte Abhängigkeiten (AWS SDK, three.js-Stack, framer-motion, pdf-parse, pdf2pic, …). | ✅ entfernt |
| I6 | Mittel | Compose reichte `APP_URL`, `SECRETS_MASTER_KEY`, E-Mail-Variablen nicht durch, dafür ungenutzte `AWS_*`. | ✅ |
| I7 | Niedrig | README/DEPLOYMENT beschrieben S3-Pflicht und Manus-OAuth als Voraussetzung (veraltet). | ✅ |
| I8 | Niedrig | Google-Fonts-Import in `index.css` (externer Request, Datenschutz). | ⏳ Vorschlag: Fonts self-hosten |
| I9 | Niedrig | Tests (`*.test.ts`) vom Typecheck ausgeschlossen. | ⏳ |

## 5. Testabdeckung (offen)

Die vorhandenen Tests prüfen überwiegend lokal nachgebaute Hilfsfunktionen statt
Produktivcode. Ohne Abdeckung: `createJournalEntry`/`approveJournalEntry`
(Doppik, Periodensperre, Belegnummern), gesamter MWST-Router, Bilanz/ER inkl.
Eröffnungssaldi (B1 wäre sofort aufgefallen), Jahresabschluss, Payroll-Buchungssatz,
`invoices.issue/cancel`, Multi-Tenant-Isolation. Empfehlung: Integrationstests
gegen die CI-MySQL (Infrastruktur existiert bereits für `core-chain`).

## 6. Empfohlene Reihenfolge für die offenen Punkte

1. B4/B5-Rest, B6, B7, B8 (Zahlenrichtigkeit MWST, Lohn, Rechnungen).
2. S19/S20, Rollenmodell innerhalb der Org (viewer/bookkeeper/admin) durchgängig.
3. B12, B16, B17, B18 (Bankabgleich und Rechnungsstellung).
4. F6, F8, F11 (Performance und UX-Konsistenz), Integrationstests (Abschnitt 5).
