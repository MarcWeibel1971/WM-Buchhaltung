import { readFileSync } from "node:fs";
import { describe, expect, it } from "vitest";
describe("BankImportDocumentPreviewDialog",()=>{it("separates document metadata and preview rendering",()=>{const component=readFileSync(new URL("../client/src/components/BankImportDocumentPreviewDialog.tsx",import.meta.url),"utf8");const page=readFileSync(new URL("../client/src/pages/BankImport.tsx",import.meta.url),"utf8");expect(component).toContain("Rechnungsvorschau");expect(component).toContain("document.aiMetadata");expect(component).toContain("application/pdf");expect(page).toContain('import { BankImportDocumentPreviewDialog } from "@/components/BankImportDocumentPreviewDialog"');expect(page).toContain("<BankImportDocumentPreviewDialog document={previewDoc} onClose={() => setPreviewDoc(null)} />");});});
