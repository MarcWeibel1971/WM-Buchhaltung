import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
const months=["Januar","Februar","März","April","Mai","Juni","Juli","August","September","Oktober","November","Dezember"];
export function CompanyVatSettingsCard({editing,value,onValueChange}:{editing:boolean;value:(key:string)=>string;onValueChange:(key:string,value:string)=>void}){const field=(k:string,l:string,p?:string)=><div><Label>{l}</Label>{editing?<Input className="mt-1" value={value(k)} onChange={e=>onValueChange(k,e.target.value)} placeholder={p}/>:<p className="mt-1">{value(k)||"—"}</p>}</div>;const method=value("vatMethod");return <Card><CardHeader><CardTitle className="text-base">MWST &amp; Geschäftsjahr</CardTitle></CardHeader><CardContent className="grid grid-cols-2 gap-4"><div><Label>MWST-Methode</Label>{editing?<Select value={method} onValueChange={v=>onValueChange("vatMethod",v)}><SelectTrigger className="mt-1"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="effective">Effektive Methode</SelectItem><SelectItem value="saldo">Saldosteuersatz</SelectItem><SelectItem value="pauschal">Pauschalsteuersatz</SelectItem></SelectContent></Select>:<p className="mt-1">{method==="effective"?"Effektive Methode":method==="saldo"?"Saldosteuersatz":method||"—"}</p>}</div>{method==="saldo"&&field("vatSaldoRate","Saldosteuersatz (%)","6.20")}{method==="pauschal"&&<>{field("vatPauschalRate","Bewilligter Pauschalsteuersatz (%)","3.5")}{field("vatPauschalActivity","Bewilligte Tätigkeit","Gemäss ESTV-Bewilligung")}</>}<div><Label>Abrechnungsperiode</Label>{editing?<Select value={value("vatPeriod")} onValueChange={v=>onValueChange("vatPeriod",v)}><SelectTrigger className="mt-1"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="quarterly">Quartalsweise</SelectItem><SelectItem value="semi-annual">Halbjährlich</SelectItem></SelectContent></Select>:<p className="mt-1">{value("vatPeriod")==="quarterly"?"Quartalsweise":"Halbjährlich"}</p>}</div><div><Label>Geschäftsjahr Beginn (Monat)</Label>{editing?<Select value={value("fiscalYearStartMonth")} onValueChange={v=>onValueChange("fiscalYearStartMonth",v)}><SelectTrigger className="mt-1"><SelectValue/></SelectTrigger><SelectContent>{months.map((m,i)=><SelectItem key={m} value={String(i+1)}>{m}</SelectItem>)}</SelectContent></Select>:<p className="mt-1">{months[Number(value("fiscalYearStartMonth")||1)-1]}</p>}</div></CardContent></Card>}
