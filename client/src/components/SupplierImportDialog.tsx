import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { Loader2, Upload } from "lucide-react";

export function SupplierImportDialog({ open, onOpenChange, onFileChange, onImport, preview, isPending }: { open: boolean; onOpenChange: (open: boolean) => void; onFileChange: (event: React.ChangeEvent<HTMLInputElement>) => void; onImport: () => void; preview: any[]; isPending: boolean }) {
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-3xl max-h-[85vh] overflow-y-auto"><DialogHeader><DialogTitle>Lieferanten importieren (CSV/Excel)</DialogTitle></DialogHeader><div className="space-y-4 py-4"><p className="text-sm text-muted-foreground">Unterstützte Spalten: <strong>Name/Firma/Lieferant</strong>, Adresse, PLZ, Ort, IBAN, E-Mail und Zahlungsfrist.</p><input id="supplier-import-file" type="file" accept=".xlsx,.xls,.csv" className="hidden" onChange={onFileChange} /><Button variant="outline" asChild><label htmlFor="supplier-import-file" className="cursor-pointer"><Upload className="h-4 w-4 mr-2" />Datei wählen</label></Button>{preview.length > 0 && <div className="border rounded-lg max-h-64 overflow-y-auto"><Table><TableHeader><TableRow><TableHead>Name</TableHead><TableHead>Adresse</TableHead><TableHead>IBAN</TableHead><TableHead>E-Mail</TableHead><TableHead>Zahlungsfrist</TableHead></TableRow></TableHeader><TableBody>{preview.slice(0, 50).map((supplier, index) => <TableRow key={index}><TableCell className="font-medium text-sm">{supplier.name}</TableCell><TableCell className="text-xs">{[supplier.street, [supplier.zipCode, supplier.city].filter(Boolean).join(" ")].filter(Boolean).join(", ")}</TableCell><TableCell className="text-xs font-mono">{supplier.iban || "–"}</TableCell><TableCell className="text-xs">{supplier.email || "–"}</TableCell><TableCell className="text-xs">{supplier.paymentTermDays ? `${supplier.paymentTermDays} Tage` : "–"}</TableCell></TableRow>)}</TableBody></Table></div>}</div><DialogFooter><Button variant="outline" onClick={() => onOpenChange(false)}>Abbrechen</Button><Button onClick={onImport} disabled={preview.length === 0 || isPending}>{isPending ? <Loader2 className="h-4 w-4 animate-spin mr-1" /> : <Upload className="h-4 w-4 mr-1" />}{preview.length} Lieferanten importieren</Button></DialogFooter></DialogContent></Dialog>;
}
