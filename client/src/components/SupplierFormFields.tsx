import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

export function SupplierFormFields({ name, onNameChange, contact, onContactChange, street, onStreetChange, zip, onZipChange, city, onCityChange, country, onCountryChange, iban, onIbanChange, bic, onBicChange, email, onEmailChange, phone, onPhoneChange }: { name: string; onNameChange: (value: string) => void; contact: string; onContactChange: (value: string) => void; street: string; onStreetChange: (value: string) => void; zip: string; onZipChange: (value: string) => void; city: string; onCityChange: (value: string) => void; country: string; onCountryChange: (value: string) => void; iban: string; onIbanChange: (value: string) => void; bic: string; onBicChange: (value: string) => void; email: string; onEmailChange: (value: string) => void; phone: string; onPhoneChange: (value: string) => void }) {
  return <><div className="grid grid-cols-2 gap-4"><div><Label>Name *</Label><Input value={name} onChange={event => onNameChange(event.target.value)} placeholder="z.B. AXA Versicherungen AG" /></div><div><Label>Kontaktperson</Label><Input value={contact} onChange={event => onContactChange(event.target.value)} placeholder="Max Mustermann" /></div></div><div><Label>Strasse</Label><Input value={street} onChange={event => onStreetChange(event.target.value)} placeholder="Musterstrasse 1" /></div><div className="grid grid-cols-3 gap-4"><div><Label>PLZ</Label><Input value={zip} onChange={event => onZipChange(event.target.value)} placeholder="6000" /></div><div><Label>Ort</Label><Input value={city} onChange={event => onCityChange(event.target.value)} placeholder="Luzern" /></div><div><Label>Land</Label><Input value={country} onChange={event => onCountryChange(event.target.value)} /></div></div><div className="grid grid-cols-2 gap-4"><div><Label>IBAN</Label><Input value={iban} onChange={event => onIbanChange(event.target.value)} className="font-mono" /></div><div><Label>BIC / SWIFT</Label><Input value={bic} onChange={event => onBicChange(event.target.value)} className="font-mono" /></div></div><div className="grid grid-cols-2 gap-4"><div><Label>E-Mail</Label><Input value={email} onChange={event => onEmailChange(event.target.value)} type="email" /></div><div><Label>Telefon</Label><Input value={phone} onChange={event => onPhoneChange(event.target.value)} /></div></div></>;
}
